import React from 'react';
import { MainContent } from '../../routes'

function Main () {
    return (
        <main className='site-content'>
            <MainContent />
        </main>
    );
}

export default Main;
